node_modules
/coverage

# The eslint plugin test fixtures contain files that deliberatly
# fail linting in order to test that the plugin reports those errors.
# We don't want the normal `eslint run` to complain about those files,
# so we ignore them here.
packages/plugin-eslint/tests/fixtures
