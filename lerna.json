{
  "version": "independent",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/tests/**"],
      "registry": "https://npm.pkg.github.com"
    }
  }
}
