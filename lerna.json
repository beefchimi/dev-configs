{
  "version": "independent",
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/tests/**"],
      "registry": "https://npm.pkg.github.com"
    }
  }
}
